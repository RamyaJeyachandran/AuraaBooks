server {
    listen 80;
    server_name books.auraazenai.com;

    location = /favicon.ico { access_log off; log_not_found off; }
    
    location /static/ {
        root /var/www/books;
    }

    location /media/ {
        root /var/www/books;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}
